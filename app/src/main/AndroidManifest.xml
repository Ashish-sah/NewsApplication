<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashish.NewsApplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ScreenTheme">
        <activity android:name=".newsExplorer"></activity>
        <activity android:name=".Science.ScienceActivity" />
        <activity android:name=".Sports.SportsActivity" />
        <activity android:name=".Health.HealthActivity" />
        <activity android:name=".EntertainmentFolder.EntertainmentActivity" />
        <activity
            android:name=".BusinessFolder.BusinessActivity"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/ScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>